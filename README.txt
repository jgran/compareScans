usage: source compareScans.sh

For now it compares two files, file1.root and file2.root, that you must provide. Could add ability to take input files as arguments.

To edit the variables in each scan or add a new scan, modify run_makeScan.C.
